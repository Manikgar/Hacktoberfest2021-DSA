#include<iostream>
using namespace std;
int main(){
    int m,n;
    int arr[][3]={{2,0,4},{2,3,4},{2,3,4}};
    cout<<"nos ";
    cin>>m>>n;
    int max= arr[0][0];
    int min= arr[0][0];
    for(int i=0; i<n;i++){
        for(int j=0;j<m;j++)
        {
            if(arr[i][j]>max)
            max=arr[i][j];
            if(arr[i][j]<min)
            min=arr[i][j];
         }
    }
        cout<<max<<min;
        
}
